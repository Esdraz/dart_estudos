void main() {
  // operadores relacionais
  // == (igualdade), != (diferença), < (menor que), > (maior que),
  // <= (menor igual), >= (maior igual).

  final idade = 18;
  final tipoPet = 'Gato';

  if (idade == 18) {
    print('pode tirar habilitação');
  }

  if (idade > 18) {
    print('pode tirar habilitação');
  }

  if (idade >= 18) {
    print('pode tirar habilitação');
  }

  if (tipoPet != 'Cachorro') {
    print('Não tempo produtos para o seu pet.');
  }
}
